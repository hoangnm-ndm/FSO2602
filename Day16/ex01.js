/**
 * {}: block code, object.
 * []: array, truy vấn đến 1 biến
 * (): gọi hàm, nhóm biểu thức, return...
 * <>: JSX, generic type.
 */

const arr = ["apple", "banana", "orange"];

arr[0];

const obj = { name: "Hoang", age: 18 };

console.log(obj["name"]);

console.log(Array.prototype);

console.log(arr.length);

arr.push("kiwi");
