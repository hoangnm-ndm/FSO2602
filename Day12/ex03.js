let myName = "Hoang"; // string
let age = 20; // number
let isStudent = true; // boolean (true/false) - dữ liệu dạng luận lí.

let myNull = null; // null - giá trị rỗng, không có giá trị nào cả.

let myUndefined; // undefined - biến đã được khai báo nhưng chưa được gán giá trị.

let hobbies = ["coding", "reading", "traveling"]; // array (mảng) - một tập hợp các giá trị.
let person = {
	name: "Hoang",
	age: 20,
	isStudent: true,
};

console.log("hoang" / 2);
console.log(typeof NaN);
